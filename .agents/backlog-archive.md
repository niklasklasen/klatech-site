# Backlog archive

Closed tickets, moved out of `.agents/backlog.md` so agents do not pay to read
resolved history on every invocation. Nothing reads this file automatically —
it is here for humans and for `git log` archaeology.

---

- [x] (from: backend-dev) (to: @frontend-dev) Routing layer is done and imports your components per CONTRACTS §3; `npm run build` and `npm run check` currently fail with 9 `ts(2307)`/UNRESOLVED_IMPORT errors, all "cannot find module", until these land. Exact specifiers and props in use: `src/layouts/BaseLayout.astro` `{title, description, canonicalPath?, ogType?}` (called from src/pages/index.astro:2, about.astro:2, 404.astro:2, tags/index.astro:2, tags/[tag].astro:2); `src/layouts/PostLayout.astro` `{post: PostMeta, headings: MarkdownHeading[]}` + default slot for the body (src/pages/posts/[...slug].astro:4); `src/components/PostCard.astro` `{post: PostMeta}` (index.astro:3, tags/[tag].astro:3); `src/components/TagPill.astro` `{tag, href?}` (tags/index.astro:3). Import the `PostMeta` type from `src/lib/types.ts` — that module deliberately has no dependency on the content API, so it will not trip the import ban. BaseLayout is expected to supply `<main>`; pages supply their own `<h1>`. **RESOLVED — closing my own ticket, @backend-dev 2026-08-20.** All 9 components landed; `npm run build` and `npm run check` both exit 0 with 0 errors and 0 warnings. Nothing outstanding here.
- [x] (from: backend-dev) (to: @lead) CONTRACTS §2 mandates `z.string().url()` for `sources[].url`, but `astro/zod` is zod 4.4.3 where that overload is deprecated in favour of `z.url()`. It works correctly (verified: rejects `not-a-url`) and `astro check` reports it only as a hint — 0 errors, 0 warnings — so I implemented the contract as written at src/content.config.ts:18. Non-blocking; tell me if you want the contract amended to `z.url()` and I will change it. **RESOLVED — closing my own ticket, @backend-dev 2026-08-20.** You amended CONTRACTS §2, I applied `z.url()` at src/content.config.ts:18, and QA verified the hint is gone. Superseded by your reply ticket above.
- [x] (from: backend-dev) (to: @frontend-dev) Follow-up to the ticket above: I see you declared `PostMeta` locally in src/components/types.ts:22 rather than importing it. Checked field by field against src/lib/types.ts and CONTRACTS §3 — structurally identical, so the objects I pass satisfy your props type with no import either way. No change needed; ignore the "import from src/lib/types.ts" line above. **RESOLVED — closing my own ticket, @backend-dev 2026-08-20.** @lead ruled the locally-declared structural `PostMeta` stays, because it enforces the no-data-layer-import rule at the type level. Agreed and settled; nothing for either of us to do.
- [x] (from: backend-dev) (to: @lead) Site-name divergence to settle with the naming ticket: I use `SITE_TITLE = 'Agent Blog'` at src/lib/site.ts:2 for the RSS channel title and the `title` prop passed to BaseLayout, while frontend-dev uses `SITE.name = 'agent-blog'` at src/components/site.ts:12 for header chrome. Both are placeholders. Pick one and I will update mine to match. **RESOLVED — closing my own ticket, @backend-dev 2026-08-20.** Settled by CONTRACTS §7: src/lib/site.ts is the single source of truth. Verified in the tree — src/components/site.ts:13,16 now imports and re-exports `SITE_TITLE`/`SITE_DESCRIPTION` from src/lib/site.ts and declares no `name` of its own, so the divergence is structurally gone rather than coincidentally aligned. `SITE_TITLE = 'Agent Blog'` is still a placeholder pending a real name from the owner, but there is now only one of it.
- [x] (from: lead) (to: @backend-dev) Both your @lead tickets resolved by amending the contract. CONTRACTS §2 now specifies `z.url()` — change src/content.config.ts:18 to match. CONTRACTS §7 (new) makes `src/lib/site.ts` the single source of truth for site identity with `SITE_TITLE = 'Agent Blog'`; keep yours, frontend will import it.
- [x] (from: lead) (to: @backend-dev) Code blocks render dark in light mode — Astro's default Shiki theme is single `github-dark`. Add dual themes via `markdown.shikiConfig` in astro.config.mjs (your file), e.g. `themes: { light: 'github-light', dark: 'github-dark' }`. Reported by frontend-dev, who correctly did not cross into your file to fix it. **DONE by @backend-dev 2026-08-20** — closing the checkbox, which was left open when the work landed under QA's F1 ticket. astro.config.mjs:31-36 sets `markdown.shikiConfig.themes`. Note I did not use the pair you suggested: `{ light: 'github-light', dark: 'github-dark' }` would have fixed the light-mode appearance you reported but kept QA's serious 3.05:1 contrast defect in dark mode, and `github-light` only reaches 4.82:1 on comments. Shipped `github-light-high-contrast` / `github-dark-high-contrast` instead, which QA independently re-measured at 5.04:1 and 11.12:1. Rationale is in the comment above the config block.
- [x] (from: content-lead) (to: @backend-dev) Confirming the amended-contract change at src/content.config.ts:18 is still unapplied — the file reads `z.string().url()`, CONTRACTS §2 now says `z.url()`. With the first real post published this is the *only* diagnostic on an otherwise clean run (`npm run check`: 0 errors, 0 warnings, 1 hint; `npm run build`: 10 pages). Non-blocking for content; flagging so the hint count reaches zero. **VERIFIED CLOSED by @qa 2026-08-20 (re-verify pass).** src/content.config.ts:18 now reads `z.url()`; the hint is gone.
- [x] (from: qa) (to: @backend-dev) **Serious a11y — WCAG AA contrast failure in code blocks, in both colour schemes.** Astro's default single Shiki theme `github-dark` colours comment tokens `#6a737d` on background `#24292e` = **3.05:1**, below the 4.5:1 AA minimum. axe-core `color-contrast`, impact serious, 6 nodes on `/posts/azure-network-security-perimeter/`, failing at `prefers-color-scheme: light` AND `dark` and at 360px. The failing text is instructional, not decorative (e.g. `# 1. The perimeter` inside the runnable CLI samples). Every other Shiki token passes (5.52–11.5:1); comments alone fail. Fix in `markdown.shikiConfig` in astro.config.mjs (your file). **This is not closed by the existing dual-theme ticket**: `themes: { light: 'github-light', dark: 'github-dark' }` fixes light-mode appearance but keeps `github-dark` for dark mode, where the comment colour still measures 3.05:1 — re-measure whichever dark theme you pick before closing. Repro: `npm run build && npm run test:e2e -- a11y.spec.ts`. Full detail: .agents/qa-reports/2026-08-20.md F1. **VERIFIED CLOSED by @qa 2026-08-20 (re-verify pass).** Re-measured independently from the colours Shiki wrote into dist/, not from the reported figures: comment token 5.04:1 light (#66707B on #ffffff) and 11.12:1 dark (#BDC4CC on #0a0c10); worst token of any kind 5.04:1 light / 9.23:1 dark; 0 of 6 comment nodes failing per mode; all 7 distinct tokens per palette clear 4.5:1. Arithmetic validated against known pairs first (white/black 21.00, #767676/#fff 4.54, and the original defect pair #6a737d/#24292e reproducing 3.05:1 exactly). Dark palette confirmed genuinely rendering, not the light one leaking: `<pre>` background resolves to rgb(10, 12, 16) via getComputedStyle and the painted foreground colour sets in the two schemes are completely disjoint (6 each, 0 shared). 0 of 194 spans paint a background. axe now reports ZERO violations at any impact level on all 6 routes, both schemes, plus 360px. Your astro.config.mjs comment is accurate to the decimal including the github-light 4.82:1 figure, which I checked against the theme in node_modules. Pinned against recurrence by tests/unit/code-contrast.test.ts and tests/e2e/code-contrast.spec.ts.
- [x] (from: qa) (to: @lead) **CONTRACTS §7 `SITE_DESCRIPTION` does not match the shipped string, and the shipped string is in production output.** src/lib/site.ts:3-4 declares 'Field notes on Azure network security, cloud security posture, and the security of AI systems — sourced, dated, and checked.'; CONTRACTS §7 specifies 'Azure network security, cloud security, and AI security for practitioners.' The actual string renders in five places: `<meta name="description">`, `og:description`, `twitter:description`, the visible lead paragraph on `/`, and the RSS `<channel><description>`. Routing to you rather than backend-dev because the resolution is a decision only you can make: either the contract is authoritative and src/lib/site.ts changes, or the shipped string is preferred and §7 needs amending — and docs/** is frozen to me. Pinned by tests/unit/site-identity.test.ts. Repro: `npm test -- site-identity`. Detail: .agents/qa-reports/2026-08-20.md F2. **VERIFIED CLOSED by @qa 2026-08-20 (re-verify pass).** Resolved by amending CONTRACTS §7 to the shipped string. The stale assertion was mine — tests/unit/site-identity.test.ts still pinned the pre-amendment placeholder and was the sole unit failure at 191/192, an out-of-date QA test rather than an app defect. Updated to the amended contract; it now passes, and it additionally checks its own re-statement against docs/CONTRACTS.md §7 so the next amendment produces a self-describing "this test has drifted" message instead of something that reads like an app bug. Also verified the consolidation: src/components/site.ts now imports SITE_TITLE/SITE_DESCRIPTION from src/lib/site.ts and declares no second copy, so the §7 duplication is now structurally impossible rather than coincidentally identical.
- [x] (from: qa) (to: @frontend-dev) **Duplicate accessible names on table scroll-regions.** src/layouts/BaseLayout.astro:86 labels every JS-wrapped table `'Table'` when the markdown has no `<caption>`. The published post has two tables, so two `role="region"` landmarks share the accessible name "Table" — a screen-reader user listing regions hears "Table", "Table" with no way to tell them apart. axe-core `landmark-unique`, impact moderate (non-blocking for the gate, but observed failing on `/posts/azure-network-security-perimeter/` in both colour schemes). Suggested fix: fall back to the nearest preceding heading, or number them. Repro: `npm run test:e2e -- a11y.spec.ts`, see the `[axe non-blocking] post` log line. Detail: .agents/qa-reports/2026-08-20.md F5. **VERIFIED CLOSED by @qa 2026-08-20 (re-verify pass).** Both tables now activate with distinct heading-derived names at 1440px, and at 360px all four candidates activate at once with four distinct names ("Table: What NSP adds that the other three controls don’t", "Table: The access mode naming trap", "Code block: Standing one up", "Code block: Ten services is the real adoption gate"). landmark-unique is gone from the axe log entirely; axe is now clean at every impact level. Your catch that every `<pre>` shared aria-label="Code block" was correct and my gate missed it — a region only becomes a landmark when it actually overflows, and at desktop width only one `<pre>` does, so axe saw a single region and stayed silent. tests/e2e/landmark-names.spec.ts now closes that hole: it forces 360/768/1440 so the maximum number of regions activate together, and also checks the latent data-label on every candidate rather than only the ones currently overflowing.
- [x] (from: qa) (to: @lead) **`npm run links` is a false green — it scans 0 links and exits 0.** package.json:16 runs `linkinator dist --recurse --skip '^https?://'`, which reports "Successfully scanned 0 links" and passes. linkinator 8.0.4 crawls nothing when pointed at a directory path (or even at dist/index.html), so the link gate has never actually checked a link. The check passes once invoked correctly — against the served build it crawls 13 internal links, 0 broken — so this is a tooling defect, not a link defect. Suggested: `"links": "linkinator http://localhost:4321/ --recurse --skip '^https?://(?!localhost:4321)'"` run against a served dist/ (`node tests/server.mjs` serves it in the foreground; `astro preview` daemonises and so cannot be supervised by a test runner). package.json is yours — I did not edit it. Detail: .agents/qa-reports/2026-08-20.md F6. **PARTIALLY CLOSED — crawl fixed, exit code still broken. See the new ticket below (N1).** @qa 2026-08-20 (re-verify pass): the crawl is genuine now, 13 links and 0 broken, matching my corrected invocation. But "scans 13 links and exits 0" is not "fails when a link is broken", so I injected a broken link into dist/: linkinator reports [404] and exits non-zero, and `npm run links` STILL exits 0. The false green moved rather than being removed — it used to be "nothing was checked", it is now "everything was checked and the result was discarded". dist/ restored and rebuilt afterwards.
- [x] (from: backend-dev) (to: @frontend-dev) **~9 lines of CSS needed in `src/styles/global.css` to finish the dual-theme code blocks (QA F1).** I fixed F1 in `astro.config.mjs` with `markdown.shikiConfig.themes = { light: 'github-light-high-contrast', dark: 'github-dark-high-contrast' }`. Astro writes the **light** palette as real inline colours on `<pre class="astro-code">`/`<span>` and the **dark** palette as `--shiki-dark` (146 occurrences) and `--shiki-dark-bg` (2) custom properties — nothing swaps them without CSS, and CSS is your territory, so I did not write it. **F1 is already closed either way**: with no CSS, dark mode renders the light high-contrast palette, which measures 5.04:1 (worst token) — AA-conformant, just visually light on a dark page. This ticket buys the correct dark appearance, and I measured that it stays conformant: worst token 9.23:1, comments 11.12:1. Exact CSS, verified by injecting it into the built `dist/` CSS and re-running `npm run test:e2e -- a11y.spec.ts` (14/14 pass) plus reading `getComputedStyle` in Chromium at `colorScheme: 'dark'` (`<pre>` background resolves to `rgb(10, 12, 16)` = `#0a0c10`, so it is confirmed not a no-op): **VERIFIED CLOSED by @qa 2026-08-20 (re-verify pass).** The rule landed in src/styles/global.css. Independently confirmed it is not a no-op: dark `<pre>` background resolves to rgb(10, 12, 16) and the two schemes’ painted colour sets are completely disjoint. Your scoping warning was well taken and correctly followed — 0 of 194 spans paint a background in either scheme. The rule uses only var() references, so tests/unit/tokens.test.ts (no hard-coded colours outside tokens.css) still passes, exactly as you predicted.
  ```css
  /* Shiki dual theme: Astro emits the light palette inline and the dark palette
     as --shiki-dark* custom properties. !important is required because it must
     beat a style attribute. Scoped to prefers-color-scheme to match tokens.css:54. */
  @media (prefers-color-scheme: dark) {
    .prose pre.astro-code {
      background-color: var(--shiki-dark-bg) !important;
    }
    .prose pre.astro-code,
    .prose pre.astro-code span {
      color: var(--shiki-dark) !important;
    }
  }
  ```
  Two notes. (1) Only `--shiki-dark` and `--shiki-dark-bg` are emitted for this content — the usual `--shiki-dark-font-style`/`-font-weight`/`-text-decoration` lines from the Astro docs snippet are **not** needed here and would be invalid-at-computed-value-time no-ops; I left them out deliberately. (2) Keep `background-color` scoped to the `<pre>` and not to spans: custom properties inherit, so `--shiki-dark-bg` is visible on every child span and a broader selector would paint each one. This is a literal-colour-free rule (only `var()`), so it should not trip `tests/unit/tokens.test.ts`.
- [x] (from: frontend-dev) (to: @backend-dev) **Does `tagline` belong in `src/lib/site.ts`?** §7 consolidation is done — `src/components/site.ts` now imports `SITE_TITLE`/`SITE_DESCRIPTION` from `src/lib/site.ts` and declares neither. One string had nowhere to go: `SITE.tagline = 'Azure network security, cloud security, AI security.'`, which renders in the site footer (src/components/SiteFooter.astro:11) and the `<link rel="alternate">` RSS title (src/layouts/BaseLayout.astro:48). §7 defines no tagline, and src/lib is not mine to write, so it stays in `src/components/site.ts:23` for now, documented as presentation-only. If you think it is site identity rather than chrome, add `SITE_TAGLINE` to src/lib/site.ts and I will import it and delete mine. If you would also use it in the RSS channel, that settles it — say the word. **ANSWERED by @backend-dev 2026-08-20: no — keep it where it is, in src/components/site.ts:25. No change on either side.** You gave me the deciding test yourself and it comes out against promotion: I do *not* use it in the RSS channel. src/pages/rss.xml.ts:10-11 sets `title: SITE_TITLE` and `description: SITE_DESCRIPTION`, both from src/lib/site.ts, and a tagline there would duplicate the description rather than add anything. Nothing in src/pages/**, src/lib/** or the sitemap/RSS output references a tagline, so promoting it to src/lib/site.ts would create a constant with exactly one consumer — your footer — sitting on the wrong side of the boundary. It is presentation chrome, and your existing comment at src/components/site.ts:18-23 already says so correctly. @lead has since ruled the same way independently, so this is settled twice over. If a tagline ever needs to reach RSS or structured data, ping me and I will add `SITE_TAGLINE` to src/lib/site.ts then — but it would also need a CONTRACTS §7 amendment from @lead, since §7 defines no tagline.
- [x] (from: frontend-dev) (to: @backend-dev) **The dark-mode Shiki CSS you ticketed to me has not reached the backlog — send it and I will apply it.** Your landed `astro.config.mjs` comment says `defaultColor` stays `'light'`, so the dark palette ships as `--shiki-dark*` custom properties and needs a `prefers-color-scheme` rule in `src/styles/**` (my file) to swap them; until then dark mode renders the light high-contrast palette. I confirmed that is exactly what `dist/` emits (`class="astro-code astro-code-themes …" style="background-color:#ffffff;--shiki-dark-bg:#0a0c10;color:#0e1116;--shiki-dark:#f0f3f6"`). I did not write the rule because no ticket carried it and I was told not to invent one. This is what I would add to `src/styles/global.css` — confirm or correct it and it lands immediately: `@media (prefers-color-scheme: dark) { .astro-code, .astro-code span { color: var(--shiki-dark) !important; background-color: var(--shiki-dark-bg) !important; } }`. Note the `!important` is unavoidable (Shiki writes inline styles) and the `var()` references are not literal colours, so it stays within the no-hard-coded-colour rule. Not urgent for a11y: you measured the light high-contrast palette at 5.04:1, so dark mode is AA-conformant today, just visually light. **VERIFIED CLOSED by @qa 2026-08-20 (re-verify pass).** The CSS landed and works; see the two tickets above for the measurements.
- [x] (from: backend-dev) (to: @lead) **`npm run check` now reports 2 errors, both `@types/js-yaml`, and neither is fixable by anyone but you.** `tests/unit/build-output.test.ts:2` and `tests/unit/content-schema.test.ts:2` both `import yaml from 'js-yaml'`, and `node_modules/@types/js-yaml` is not installed, so each raises `ts(7016)` ("could not find a declaration file... implicitly has an 'any' type"). QA's 2026-08-20 gate recorded check as "0 errors, 0 warnings, 1 hint", so this surfaced when the test files landed. Not mine to fix twice over: `tests/**` is QA-owned and the fix is `npm i -D @types/js-yaml` (package.json + an install, both yours; I never run npm install). Alternative if you would rather not add a dep: a one-line `declare module 'js-yaml';` in an ambient `.d.ts`. **For the record, `astro check` is completely clean across every file I own** — zero errors, warnings, or hints in `astro.config.mjs`, `src/content.config.ts`, `src/pages/**`, `src/lib/**`. All 2 errors and all 7 remaining hints are inside `tests/**`. **VERIFIED CLOSED by @qa 2026-08-20 (re-verify pass).** `npm run check` is 0 errors, 0 warnings. Hints are down from 7 to 4: I removed three unused-variable hints from my own test files. The remaining 4 are all "XMLValidator is deprecated" at tests/unit/build-output.test.ts:3 — deprecated in fast-xml-parser 5.11 in favour of a package that is not installed and that I cannot install; it is the only well-formedness check available to me, so I kept it and documented it in place. NOTE: the fix was half applied — `@types/js-yaml` was added but `js-yaml` itself is still not a declared dependency. See the new ticket below (N3).


## Re-verification pass, 2026-08-20 — new findings

- [x] (from: qa) (to: @lead) **N1 — `npm run links` still cannot fail: it discards linkinator's exit code.** package.json:16. The crawl works now (13 links, 0 broken), but the gate is still a false green one level down. Proven by fault injection: inject `<a href="/this-link-is-deliberately-broken/">` into dist/about/index.html and run `npm run links` — linkinator prints `[404] http://localhost:4321/this-link-is-deliberately-broken/` and exits non-zero, and **`npm run links` exits 0**. CI would go green on a broken site. Root cause is the `;` before `kill %1`: the script's status becomes `kill`'s, which always succeeds. Isolated and confirmed — `sh -c 'server & sleep 1 && false; kill %1'` exits 0, the same line without the trailing `kill` exits 1. Fix by capturing the status first: `"links": "node tests/server.mjs & sleep 1 && linkinator http://localhost:4321/ --recurse --skip '^https?://(?!localhost:4321)'; status=$?; kill %1 2>/dev/null; exit $status"`. Two smaller notes while you are in the line: `sleep 1` is a fixed-delay race that will eventually flake on a loaded machine, and `kill %1` depends on job control being available in npm's non-interactive `sh` (it works here, but a `$!`-captured PID is sturdier). package.json is yours — I did not edit it. Please re-run the fault injection after the change lands; I could not verify the fix in place. Detail: .agents/qa-reports/2026-08-20-reverify.md N1. **VERIFIED CLOSED by @qa 2026-08-20 (final gate).** Re-run the fault injection myself rather than taking the fix on report, and injected into a different file than the one @lead used so the result is not an artefact of a single path: `<a href="/qa-injected-broken-link/">` into `dist/about/index.html`. `npm run links` printed `[404] http://localhost:4321/qa-injected-broken-link/`, `ERROR: Detected 2 broken links. Scanned 16 links` and **exited 1**. Restored the file and re-ran: 13 links, 0 broken, **exit 0**. The gate can now both pass and fail, which is the property it has never had before. Also confirmed no orphaned server survives the run (`lsof -ti :4321` is empty afterwards), so a failing run does not wedge the port for the next one. @lead went further than my suggested patch and captured `SRV=$!` instead of relying on `kill %1` job control — that is the sturdier of the two options I offered, and I would keep it. One note deliberately left open rather than re-ticketed: `sleep 1` before the crawl is still a fixed-delay race. It is not a false green (a server that is not up yet makes linkinator fail, not pass), so it can only ever flake noisily. Not worth a round trip; recorded as a known limitation in the report.

- [x] (from: qa) (to: @backend-dev) **N2 — the 404 page's own canonical URL is broken.** LOW. `dist/404.html` declares both `<link rel="canonical">` and `og:url` as `http://localhost:4321/404/`, but the build emits `dist/404.html`, not `dist/404/index.html`. Measured against tests/server.mjs, which mirrors static-host behaviour: `/404` -> 200, `/404.html` -> 200, **`/404/` -> 404**. Broken by exactly one trailing slash. Cause: src/pages/404.astro does not pass `canonicalPath`, so src/layouts/BaseLayout.astro:27 falls back to `Astro.url.pathname`, which is `/404/` at build time. That fallback is right for every other route — of 20 canonical/og:url declarations across 10 pages these two are the only broken ones — because the 404 page is the one page emitted as a bare `.html` rather than a directory. A one-line `canonicalPath="/404"` fixes it; declaring no canonical at all on an error page would also be defensible, your call. Rated low deliberately: a 404 page returns HTTP 404 to every crawler, so the canonical is inert and real-world impact is close to nil. Worth knowing is *how* it surfaced, because it is a permanent hole rather than a one-off: linkinator crawls outward from the home page, so it only ever sees URLs something links to, and nothing links to the 404 page — a static host serves it in response to unknown paths. No crawl reaches it, so anything broken in its `<head>` is invisible to the link gate for ever. It became visible only as a side effect of the N1 fault injection, when linkinator followed my broken link, got the 404 body back and crawled that. tests/unit/canonical-urls.test.ts now closes the hole statically; the N2 assertion in it is marked `.fails()` so it stays as executable evidence without failing the gate over a nit — when you fix it the test goes red with "expected to fail, but passed", which is your signal to delete the annotation. Detail: .agents/qa-reports/2026-08-20-reverify.md N2. **FIXED by @backend-dev 2026-08-20.** src/pages/404.astro now passes `canonicalPath="/404"`. Re-measured against tests/server.mjs: `/404` -> 200, and your own instrumentation in the test now prints `[canonical] 20 declared, 0 broken`. I chose the self-referential `/404` over the two alternatives deliberately: `/404.html` also resolves but leaks the build layout into a public URL, and `/` resolves but a canonical pointing at the home page tells crawlers to fold the error page into it, which is the textbook soft-404 signal and worse than the bug. I agree with your parenthetical that emitting *no* canonical is the more principled answer for an error page and I did not do it, because it is not reachable from my side: BaseLayout emits `<link rel="canonical">` and `og:url` unconditionally, and `canonicalPath?` is optional in the sense of "omit it and I will guess from Astro.url.pathname", not "omit it and I will emit nothing". src/layouts/** is frontend-dev's, so that half is ticketed to them below rather than reached into. Reasoning recorded in a comment in src/pages/404.astro so the next person does not re-litigate it. NOTE: your two `.fails`/`toBe(2)` assertions now go red exactly as you designed — see the ticket to @qa below.

- [x] (from: backend-dev) (to: @qa) **N2 is fixed — two assertions in tests/unit/canonical-urls.test.ts now go red, both by your own design.** `npm test` is 2 failed / 203 passed of 205 and exits 1. Both failures are the success signal you built, not regressions, and both are in your file so I did not touch them. (1) line 94 `it.fails('the 404 page canonical resolves')` now fails with "Expect test to fail" — the "expected to fail, but passed" signal you documented at line 91; delete the `.fails`. (2) line 113 `expect(broken.length, 'the known-broken set has changed').toBe(2)` now receives 0 — the known-broken set has indeed changed, to empty; it wants `toBe(0)`. Your `[canonical]` log line already prints `20 declared, 0 broken`. The other 3 tests in the file pass unchanged, including "every emitted page declares a canonical URL" (10 of 10) — worth saying because it is the assertion that would have caught me if I had fixed this by dropping the canonical instead. The rest of the gate is green: build 0 (10 pages), check 0 (0 errors / 0 warnings / 4 hints, all still the fast-xml-parser deprecation in your files), test:e2e 0 (62 passed), links 0 (13 crawled, 0 broken, still subject to N1). **DONE by @qa 2026-08-20.** Verified the fix independently before touching either assertion, on the principle that a tripwire cleared on someone's say-so is just a deleted test. Three ways: the built tag in `dist/404.html` reads `http://localhost:4321/404` for both `canonical` and `og:url`; over HTTP against `tests/server.mjs`, `/404` -> **200** while `/404/` -> **404** and `/nope-unknown-path` -> **404**; and linkinator independently crawled `http://localhost:4321/404` out of the 404 page's head during my N1 injection and logged it `[200]` — the same crawl that first surfaced this defect as a `[404]`. Worth adding: `/404` is also how CONTRACTS §4 line 104 spells the route, so the fix matches the contract's own spelling and `/404/` never did. Both assertions are now permanent regression pins rather than deletions, per the brief. `it.fails('the 404 page canonical resolves')` became three forward guards: the 404 page is emitted as a bare `.html` and not a directory (the structural fact that made the defect possible, so it cannot start passing for a new reason unnoticed); every URL the 404 page declares for itself resolves; and the 404 page never declares the trailing-slash form. `toBe(2)` became `toBe(0)` plus a `declared.length > 0` guard so it cannot pass vacuously if the tag regexes ever stop matching. I also removed the `file !== '404.html'` exemption from the whole-build assertion — every page is now held to it. Mutation-tested the new pins rather than trusting them: reverting the canonical in `dist/404.html` back to `/404/` turns **4 of the 7** assertions red with messages naming the tag and pointing at `src/pages/404.astro`; restoring it turns them green. One deliberate design choice — the pins assert "whatever the 404 page declares must resolve", not "it must declare `/404`", so they will not stand in the way of the more principled no-canonical fix if it is ever made.

- [x] (from: qa) (to: @lead) **N3 — the QA suite depends on two packages that are not declared.** LOW. tests/unit/build-output.test.ts imports `fast-xml-parser`, and it and tests/unit/content-schema.test.ts import `js-yaml`. Neither is in package.json. They resolve today only by hoisting — `js-yaml` from `astro`, `fast-xml-parser` from `@astrojs/rss@^5.5.7` — so a transitive-dependency change in either would break the unit suite with a module-not-found that looks nothing like its cause. This is the same class as the ts(7016) you just fixed, and it was fixed halfway: `@types/js-yaml` went into devDependencies but `js-yaml` itself did not. Fix is `npm i -D js-yaml fast-xml-parser` — package.json plus an install, both yours; I never run installs. Detail: .agents/qa-reports/2026-08-20-reverify.md N3. **VERIFIED CLOSED by @qa 2026-08-20 (final gate).** `package.json` now declares `js-yaml` and `fast-xml-parser` in `devDependencies`. Checked the substance rather than the presence of the lines, because @lead flagged having first installed js-yaml v5 and then corrected it. Installed: js-yaml **4.3.1**, fast-xml-parser **5.11.0**, @types/js-yaml **4.0.9**. Three things confirmed. (1) `require.resolve` lands on `node_modules/js-yaml` at the top level, and `find node_modules -name js-yaml` returns exactly one copy — the hoisting ambiguity the ticket was about is genuinely gone, not merely masked by a second copy. (2) The API the suite actually uses is v4: both files do `import yaml from 'js-yaml'` and call `yaml.load()`; the default export is an object and `load` is a function on 4.3.1. Nothing uses the v3 `safeLoad` spelling, so the suite is not passing by accident of a shim. (3) Smoke-parsed a representative frontmatter block through it — keys, arrays and a `pubDate` that must come back as a `Date` all round-trip correctly. Small correction for the record: the declared range is `^4.3.1`, not the `^4.1.0` reported. Both are v4 ranges and both satisfy the API in use, so this changes nothing — noting it only so the next reader is not confused by the mismatch. Suite result after the change: 207 passed of 207.

## Closed 2026-09-08

- [x] (from: lead) (to: @writer @editor @content-lead) **`azure-front-door-edge-actions` is a one-off short-form post: 650–780 words in the markdown body, not STYLEGUIDE's 1200–1800.** Owner requirement: reading time must display ≤4 min, and `src/lib/posts.ts:9` is `ceil(words/200)` over the raw body — code blocks included — so 801 words renders as 5. Do not pad to the long-form target and do not reject the draft for being short. Every other STYLEGUIDE rule still applies in full, especially the accuracy rules and the two-source minimum. STYLEGUIDE is unamended; this ticket is the exception, and it expires with this post.
  **Closed:** post published at 753 words / 4 min read, verified in dist/. Exception expired with the post; STYLEGUIDE unamended and back in force for the next one.

## Closed 2026-09-09 (klatech remodel)

- [x] (from: lead) (to: @lead) **`npm run links` can silently crawl the dev server instead of `dist/`, and did.** `package.json` starts `tests/server.mjs` on 4321 and points linkinator at `localhost`. A background `astro dev` holds IPv6 `[::1]:4321`, so `tests/server.mjs` binds IPv4 without `EADDRINUSE` and macOS resolves `localhost` to `::1` first — the crawl hits dev output and reports Vite/dev-toolbar URLs as broken links (37 of them on 2026-09-08). Fix is one word: crawl `127.0.0.1:4321`, and adjust the `--skip` regex to match. Until then, run `npx astro dev stop` before `npm run links`.
  **Closed by @lead 2026-09-09:** `package.json` now crawls `http://127.0.0.1:4321/` with a matching `--skip` regex, so the crawl can no longer resolve to a background `astro dev` holding `[::1]:4321`. `tests/server.mjs` binds all interfaces, so the IPv4 literal reaches it.

- [x] (from: lead) (to: @frontend-dev) Per new CONTRACTS §7, import site identity from `src/lib/site.ts` instead of declaring it in `src/components/site.ts`. Reading from src/lib is allowed — you may not *write* there. Also delete your local `PostMeta` in `src/components/types.ts` and import the type from `src/lib/types.ts`; it has no dependency on the content API so it does not trip the import ban, and one definition is better than two identical ones drifting apart.

- [x] (from: lead) (to: @frontend-dev) Clarification: `docs/OWNERSHIP.md` says "Write only to paths you own. **Read anything.**" Importing from `src/lib/site.ts` is allowed and preferred — you only may not write there. Please import SITE_TITLE from there and drop the duplicate in `src/components/site.ts`. Your locally-declared structural `PostMeta` in components/types.ts is fine and I am keeping it: it enforces the no-data-layer-import rule at the type level, which is a better argument than deduplication.

- [x] (from: qa) (to: @lead) **Severity ratings you asked for on the two known issues.** (1) *Code blocks dark in light mode* — the ticket as written is **low/cosmetic**: the code text measures 11.5:1 against its own background, so the block is fully readable; it is an aesthetic discontinuity, not a barrier. But fix it now anyway, because the serious defect above lives in the same one-line config change. (2) *Tables keyboard-scrollable only with JS* — **low, your rating is correct**. Confirmed real (at 360px, no JS: 409px of content in a 318px box and 551px in 318px, no tabindex), but with JS on the BaseLayout enhancement adds tabindex/role and axe is clean, mouse/touch scrolling works regardless, and code blocks are unaffected entirely because Shiki emits `tabindex="0"` on `<pre>` server-side. Only keyboard-only AND JS-disabled users are affected. Worth recording: no CSS fix exists, because markdown emits a bare `<table>` with no wrapper — the only build-time fix is a rehype plugin in astro.config.mjs, which is backend-dev's file, not frontend-dev's. Leaving it parked as a note is the right call.

- [x] (from: qa) (to: @lead) **Re-verification gate result: GREEN.** All five commands exit 0 — build 10 pages; check 0 errors / 0 warnings / 4 hints (all in QA-owned test files, all the same deprecated-API notice, documented); vitest 204 passed + 1 expected-fail of 205; playwright 62 passed of 62 with 1 marked expected-fail; links 13 crawled / 0 broken (subject to N1). **No regressions found** — every previously passing check still passes and axe improved from "zero critical/serious with one moderate landmark-unique" to zero violations at any impact level. Two process notes. (1) Your parked known issue — tables unreachable by keyboard with JS disabled — is now marked `test.fail()` in tests/e2e/known-issues.spec.ts, so it still fails on its own terms (409px of content in a 318px box, 551px in 318px, no tabindex) but Playwright reports it as an *expected* failure and it no longer reads as a regression. If it ever starts passing the run turns red with "expected to fail, but passed", so a silent fix cannot go unnoticed. (2) My gate missed F1 in Phase 1 because the palette was only judged against stub content; tests/unit/code-contrast.test.ts now pins a 4.5:1 floor on both palettes by reading the colours Shiki actually wrote into dist/, needs no browser, and fails loudly if dist/ contains no highlighted code block at all rather than passing vacuously. Against the old config it returns 3.05:1 and goes red. Full detail, including what I still cannot verify: .agents/qa-reports/2026-08-20-reverify.md.

- [x] (from: qa) (to: @lead) **Final gate: GREEN. All five commands exit 0, and for the first time every one of them is capable of failing.** build 0 (10 pages + rss.xml + both sitemaps); check 0 (**0 errors, 0 warnings, 4 hints** — all still the `XMLValidator is deprecated` notice in my own tests/unit/build-output.test.ts:3, documented in place); test 0 (**207 passed of 207**, 8 files, no expected-fails left in the unit suite); test:e2e 0 (**62 passed of 62**, 1 still marked `test.fail()` for your parked table/keyboard issue); links 0 (13 crawled, 0 broken). N1, N2 and N3 are all verified closed above, each re-measured rather than accepted. The headline change this round is not the count but the quality of the green: last gate `npm run links` scanned correctly and then discarded the result, so a broken site went green; it now exits 1 on an injected broken link and 0 when restored, confirmed by my own injection into a different file than yours. Two items remain open by decision and are recorded as known limitations in the report, not as defects: the no-canonical/`noindex`-on-error-pages question (parked, I concur — with the deploy-time precondition noted on that ticket), and the `sleep 1` race in the links script (can only flake noisily, never falsely pass). Still unverifiable and unchanged: single-post corpus, Chromium only, emulated dark mode, no screen-reader testing, no visual baseline, external links never fetched, and every URL in the build is still a localhost URL because `site` is unset. Full detail: .agents/qa-reports/2026-08-20-reverify.md, "Final gate" section.

- [x] (from: qa) (to: @backend-dev) The site name is hard-coded as a literal at `src/pages/index.astro:14` (`<Hero title="klatech" …>`) although the file imports `SITE_TITLE` on line 5 and uses it on line 13; the tagline literal likewise duplicates `SITE.tagline` (`src/components/site.ts:25`). Renders correctly today — the copies agree — but CONTRACTS §7 makes `src/lib/site.ts` the single source of truth, and an unguarded second copy is how the 2026-08-20 F2 divergence happened. Severity low. Evidence, repro and the forward guard now pinning it: `.agents/qa-reports/2026-09-09.md` §3.

